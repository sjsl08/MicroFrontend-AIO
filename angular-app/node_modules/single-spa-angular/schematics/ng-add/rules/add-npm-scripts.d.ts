import { workspaces } from '@angular-devkit/core';
import { Rule } from '@angular-devkit/schematics';
import { Schema as NgAddOptions } from '../schema';
export declare function addNPMScripts(workspace: workspaces.WorkspaceDefinition, project: workspaces.ProjectDefinition, host: workspaces.WorkspaceHost, options: NgAddOptions): Rule;
